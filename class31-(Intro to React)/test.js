const calc = require('./calculator');

calc.addition(2,3);
calc.divisio(10,2);
calc.substarction(3,3);
calc.multiplication(5,5);

//This is called modularity ,when from one file we are exporting methods or function 
//to another and using their logic in another file so this whole method is called as modularity.